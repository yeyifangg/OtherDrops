# Eventually this file should include an easy test for almost every feature

defaults:

otherblocks:
  # Test block drop
  GRASS:
    - tool: ALL
      drop: GRASS
# test event - lightning
      event: [LIGHTNING]
# test messages & color
      message: ["§EYellow", "§CRed", "Plain"]

# test second drop with spade
    - tool: ANY_SPADE
      drop: APPLE
# test permissions
      permissions: "grass.apple"

# test lower chance exclusive drop
    - tool: ALL
      drop: DIAMOND
      exclusive: 1
      chance: 10
      
      
  # Test creature drop
  CREATURE_COW:
    - tool: ALL
      drop: MILK_BUCKET